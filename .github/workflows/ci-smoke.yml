name: Smoke Tests

on:
  push:
    branches:
      - main
  pull_request:
  schedule:
    - cron: "0 15 * * 1"

permissions:
  contents: read

jobs:
  smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: |
            package-lock.json
            web/package-lock.json
            functions/package-lock.json
            studio-brain/package-lock.json

      - name: Install root deps
        run: npm ci

      - name: Policy docs lint
        run: node website/scripts/lint-policies.mjs

      - name: Install web deps
        run: npm ci --prefix web

      - name: Install functions deps
        run: npm ci --prefix functions

      - name: Install studio-brain deps
        run: npm ci --prefix studio-brain

      - name: Functions lint
        run: npm --prefix functions run lint

      - name: Studio Brain policy lint
        run: npm --prefix studio-brain run lint:policy

      - name: Web lint
        run: npm --prefix web run lint

      - name: Web tests
        run: npm --prefix web run test:run

      - name: Web accessibility smoke
        run: npm --prefix web run a11y:smoke

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Website Playwright smoke
        run: npm run website:smoke:playwright

      - name: Upload website smoke artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: website-playwright-smoke
          path: output/playwright
          if-no-files-found: ignore
          retention-days: 7

      - name: Web build
        run: npm --prefix web run build

      - name: Web chunk budgets
        run: npm --prefix web run perf:chunks

      - name: Functions build
        run: npm --prefix functions run build
