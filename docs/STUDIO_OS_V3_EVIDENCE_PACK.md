# Studio OS v3 Operational Evidence Pack

Date: 2026-02-17  
Owner: micah  
Status: Local harness execution complete (staging rerun still pending)

## Execution artifact
- `output/drills/studio-os-v3-local-2026-02-17T17-47-52-585Z.json`
- Generated by `node scripts/run-studio-os-v3-local-drills.mjs`
- Harness target: `http://127.0.0.1:8788`

## Scenario outcomes (2026-02-17 local harness)
1. `token_compromise`
   - Outcome: `success`
   - MTTR: `4` minutes
   - Evidence: `/api/ops/drills` rows present (`started`, `completed`)
   - Risk: none currently open from local harness run
2. `connector_outage`
   - Outcome: `partial`
   - MTTR: `9` minutes
   - Evidence: `/api/ops/drills` + `/api/ops/audit` degraded enter/exit rows present
   - Risk: `connector-retry-threshold-tuning`
3. `policy_bypass_attempt`
   - Outcome: `success`
   - MTTR: `6` minutes
   - Evidence: `/api/ops/drills` rows present (`started`, `completed`)
   - Risk: none currently open from local harness run
4. `local_db_corruption`
   - Outcome: `partial`
   - MTTR: `12` minutes
   - Evidence: `/api/ops/drills` + `/api/ops/audit` degraded enter/exit rows present
   - Risk: `db-restore-runbook-step-order`

## API evidence summary
- `GET /api/ops/drills?limit=100`: scenario rows present for all four scenario IDs.
- `GET /api/ops/audit?limit=100&actionPrefix=studio_ops.`: contains `studio_ops.drill_event`, `studio_ops.degraded_mode_entered`, `studio_ops.degraded_mode_exited`.
- `GET /api/capabilities/audit?limit=100`: capability rows captured (kill-switch toggle + delegation denial events).

## Follow-up owners and due dates
1. `connector-retry-threshold-tuning`
   - Owner: Micah
   - Due: 2026-02-24
   - Action: tune connector health timeout/backoff and rerun outage drill.
2. `db-restore-runbook-step-order`
   - Owner: Micah
   - Due: 2026-02-24
   - Action: tighten restore sequence in the runbook and re-run tabletop.

## Remaining release gate
- Re-run same four scenarios in staging with real staff auth context and attach equivalent artifacts.
- One-shot staging runner is ready:
  - `node scripts/run-studio-os-v3-staging-drills.mjs`
  - Required env: `STUDIO_BRAIN_ID_TOKEN` (staff bearer). Optional override: `STUDIO_BRAIN_BASE_URL`.
