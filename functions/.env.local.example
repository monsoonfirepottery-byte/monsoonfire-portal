# Copy this file to functions/.env.local and fill values for local emulator runs.
# This file is ignored by git via .gitignore.

# Dev-only admin token gate (emulator)
ALLOW_DEV_ADMIN_TOKEN=true
ADMIN_TOKEN=replace_with_local_dev_token

# Optional CORS allowlist for local testing
ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Canonical emulator host contract (local loopback profile)
FIREBASE_AUTH_EMULATOR_HOST=127.0.0.1:9099
FIRESTORE_EMULATOR_HOST=127.0.0.1:8080

# Optional relay vars for push testing
# APNS_RELAY_URL=https://your-relay.example.com/send
# APNS_RELAY_KEY=replace_with_local_relay_key

# Integration tokens (PATs)
# Generate a long random value (do not commit). Used to hash PAT secrets.
INTEGRATION_TOKEN_PEPPER=replace_with_long_random_secret

# Agent client API keys
# Separate pepper for agent client key hashing.
AGENT_CLIENT_KEY_PEPPER=replace_with_long_random_secret

# Delegated agent auth tokens (short-lived, scoped, one-time nonce protected)
DELEGATED_AGENT_TOKEN_SECRET=replace_with_long_random_secret
DELEGATED_TOKEN_AUDIENCE=monsoonfire-agent-v1
