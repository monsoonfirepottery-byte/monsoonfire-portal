Legacy Open Memory runtime/ops scripts were moved here during the corpus-first pivot.

They are kept for historical reference and possible recovery work, but they are not part of the active architecture or the default command surface anymore.

Active work should target the canonical corpus exporters, SQLite materialization, query helpers, and thin adapter surfaces instead.
