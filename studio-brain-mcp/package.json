{
  "name": "studio-brain-mcp",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Standalone MCP bridge for Studio Brain memory and loop endpoints.",
  "scripts": {
    "start": "node ./server.mjs",
    "smoke": "node ./smoke.mjs",
    "test": "node --test ./server.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "zod": "^3.25.76"
  }
}
