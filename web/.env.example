# web/.env.example
# Copy to .env.local to override defaults for local dev.
# NOTE: .env.local should NOT be committed.

# Emulator toggles
# - Prefer split flags so you can run Firestore/Functions emulators while using REAL Firebase Auth.
# - Legacy `VITE_USE_EMULATORS=true` still works as a catch-all.
VITE_USE_EMULATORS=
VITE_USE_AUTH_EMULATOR=
VITE_USE_FIRESTORE_EMULATOR=
VITE_AUTH_EMULATOR_HOST=127.0.0.1
VITE_AUTH_EMULATOR_PORT=9099
VITE_FIRESTORE_EMULATOR_HOST=127.0.0.1
VITE_FIRESTORE_EMULATOR_PORT=8080

# Cloud Functions base URL.
# Defaults to production if unset:
#   https://us-central1-monsoonfire-portal.cloudfunctions.net
#
# For emulators (example):
#   http://127.0.0.1:5001/<YOUR_FIREBASE_PROJECT_ID>/us-central1
#
# If you use localhost emulators, prefer 127.0.0.1 to avoid occasional IPv6/localhost weirdness.
VITE_FUNCTIONS_BASE_URL=

# Optional: override Firebase Auth handler domain.
# Use when you introduce a custom Hosting-backed auth handler domain such as:
#   auth.monsoonfire.com
#
# Default:
#   monsoonfire-portal.firebaseapp.com
VITE_AUTH_DOMAIN=

# Dev-only: enable admin-token input in the web UI (emulator + localhost only).
# Must be "true" to show the field in dev builds.
VITE_ENABLE_DEV_ADMIN_TOKEN=false
