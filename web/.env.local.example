# Copy to web/.env.local, then choose one profile path explicitly.

# Defaults are production-safe until emulator profile is intentionally enabled.
VITE_USE_EMULATORS=false
VITE_USE_AUTH_EMULATOR=false
VITE_USE_FIRESTORE_EMULATOR=false

# Local loopback emulator profile (enable toggles above when using this):
# VITE_AUTH_EMULATOR_HOST=127.0.0.1
# VITE_AUTH_EMULATOR_PORT=9099
# VITE_FIRESTORE_EMULATOR_HOST=127.0.0.1
# VITE_FIRESTORE_EMULATOR_PORT=8080
# VITE_FUNCTIONS_BASE_URL=http://127.0.0.1:5001/monsoonfire-portal/us-central1
# VITE_STUDIO_BRAIN_BASE_URL=http://127.0.0.1:8787

# LAN/static emulator profile (enable toggles above when using this):
# VITE_AUTH_EMULATOR_HOST=studiobrain.local
# VITE_AUTH_EMULATOR_PORT=9099
# VITE_FIRESTORE_EMULATOR_HOST=studiobrain.local
# VITE_FIRESTORE_EMULATOR_PORT=8080
# VITE_FUNCTIONS_BASE_URL=http://studiobrain.local:5001/monsoonfire-portal/us-central1
# VITE_STUDIO_BRAIN_BASE_URL=http://studiobrain.local:8787

# Optional Vite host and port overrides (defaults resolve from network profile + strict port 5173).
# VITE_DEV_HOST=127.0.0.1
# VITE_PORT=5173
# Optional additional Vite allowed hosts (comma-separated)
# VITE_ALLOWED_HOSTS=localhost
# Optional explicit proxy targets (defaults derive from profile/base URL contract values)
# VITE_FUNCTIONS_PROXY_TARGET=http://127.0.0.1:5001/monsoonfire-portal/us-central1
# VITE_STUDIO_BRAIN_PROXY_TARGET=http://127.0.0.1:8787

# Development-only feature behavior toggles
VITE_DEBUG_TOOLS=true
VITE_DASHBOARD_USE_MOCK_KILN_DATA=false
# Non-dev override only (requires explicit acknowledgement token):
# VITE_DASHBOARD_MOCK_KILN_DATA_ACK=ALLOW_NON_DEV_MOCK_DATA
VITE_DASHBOARD_MOCK_KILN_DATA_ACK=
